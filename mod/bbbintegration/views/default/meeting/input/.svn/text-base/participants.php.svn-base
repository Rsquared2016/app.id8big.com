<?php

/**
 * BigBlueButton Integration
 */

if (!isset($vars['name'])) {
    $vars['name'] = 'participants';
}

$vars['options_values'] = meeting_get_participants_options();

echo elgg_view('input/dropdown', $vars);
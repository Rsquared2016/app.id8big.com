<?php

/**
 * BigBlueButton Integration
 */

if (!isset($vars['name'])) {
    $vars['name'] = 'timezone';
}

if (!isset($vars['is_pulldown'])) {
    $vars['is_pulldown'] = FALSE;
}

echo elgg_view('events/input/timezone_entities', $vars);

//$vars['options_values'] =  elgg_timezone_get_timezones($vars['is_pulldown']);
//
//echo elgg_view('input/dropdown', $vars);